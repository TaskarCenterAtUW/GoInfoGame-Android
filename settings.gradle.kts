include(":app")

pluginManagement {
    repositories {
        google() // Required for Google plugins like google-services
        gradlePluginPortal() // Required for community plugins
        mavenCentral()
    }
}
